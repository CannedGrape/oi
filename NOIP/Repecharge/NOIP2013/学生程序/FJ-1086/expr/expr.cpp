#include <iostream>#include <stdio.h>#include <stdlib.h>#include <math.h>#include <fstream>#include <algorithm>#include <cstring>using namespace std;char expr[1000000];class Symbol{      char sym;      int location;public:       Symbol(char s, int location);       Symbol();};Symbol::Symbol(char s, int location):sym(s), location(location){}int main(){    Symbol (*symArray)[100000];    int ap = 0;    cin.getline(expr, 1000000);    int length = strlen(expr);    for(int i = 0;i < length;i++){            if(expr[i] == '*' || expr[i] == '+'){                       symArray            }    }    return 0;}