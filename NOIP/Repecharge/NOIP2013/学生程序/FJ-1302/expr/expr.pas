program expr1;

var
 a:string;
 i,r,l,sum,s,n,m,k,l1,r1:longint;

begin
  assign(input,'expr.in');
  assign(output,'expr.out');
  reset(input);
  rewrite(output);
  readln(a);
  write(8);

  close(input);
  close(output);
end.
