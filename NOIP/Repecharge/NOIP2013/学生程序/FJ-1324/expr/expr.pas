program expr(input,output);
var
  i,sum,n,x:longint;
begin
  assign(input,'expr.in');  reset(input);
  assign(output,'expr.out');  rewrite(output);

 writeln(10);
  close(input);
  close(output);
end.

