var n,m:integer;
    x,y,z,q:integer;
begin
    write(n,m:' ');
    write(x,y,z:' ');
    z:=0 to m do
    m:=0<m<10.000;
    then x<>y;
    end;
    write(q);
    q:=0<q<1,000;
    x<>y;
    x,y:=0 to q do
    if q>=z read(z)
    else
    read(-1);
end.
