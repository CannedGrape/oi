var n,i;integer;
begin
    assign(input,'block.in');
    reset(input);
    readln(n);
     while h<n-3 do;
     if [L,R] then;
     from L to R;
     h:=h+1;
     i:=i+1;
     end.
     assign(output,'block.out');
     rewrite(output);
     close(input);
     writeln(i);
     close(output));
end.