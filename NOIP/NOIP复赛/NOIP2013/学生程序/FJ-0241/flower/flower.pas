var n,m,i;integer;
begin
    assign(input,'flower.in');
    reset(input);
    readln(n);
     if i>i+1 and i>i-1 then;
     m:=m-1;
     or i:=i+1;
     end.
    close(input);
    assign(output,'fiower.out');
    reset(output);
    writeln(m);
    close(output);
end.

